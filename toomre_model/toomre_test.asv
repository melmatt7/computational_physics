%Galaxy array
galaxy(1).core_m = 10;
galaxy(1).star_num = 6;

galaxy(2).core_m = 10;
galaxy(2).star_num = 11;

galaxy_system.galaxy_num = length(;
galaxy_system.tot_m = 0;
galaxy_system.r = 5;

for i = 1:length(galaxy)
    galaxy_system.core_ms(i) = galaxy(i).core_m;
    galaxy_system.tot_m = galaxy_system.tot_m + galaxy(i).core_m;
end

galaxy_system.core_ri = [(galaxy_system.core_ms(2)/galaxy_system.tot_m)*galaxy_system.r, ... 
                         (galaxy_system.core_ms(1)/galaxy_system.tot_m)*galaxy_system.r];

galaxy_system.core_vi = [sqrt(galaxy_system.core_ms(2)*galaxy_system.r)/galaxy_system.r, ...
                         sqrt(galaxy_system.core_ms(1)*galaxy_system.r)/galaxy_system.r];

tmax = 5;
level = 6;
toomre(tmax, level, galaxy_system)

